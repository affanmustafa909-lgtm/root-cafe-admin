# Production / deployed admin — point at the live Railway API.
VITE_API_URL=https://backend-root-cafe-main-production.up.railway.app

# Local-only alternative (Vite proxy):
# VITE_API_URL=http://localhost:3000
