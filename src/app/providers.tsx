import{QueryClient,QueryClientProvider}from'@tanstack/react-query';import{useState,type ReactNode}from'react';import{AuthProvider}from'@/features/auth/AuthContext';import{Toaster}from'@/shared/ui';export function Providers({children}:{children:ReactNode}){const[c]=useState(()=>new QueryClient({defaultOptions:{queries:{staleTime:15000,retry:1}}}));return <QueryClientProvider client={c}><AuthProvider><Toaster>{children}</Toaster></AuthProvider></QueryClientProvider>}
